import { AppShellNoDirective } from './app-shell-no.directive';

describe('AppShellNoDirective', () => {
  it('should create an instance', () => {
    const directive = new AppShellNoDirective();
    expect(directive).toBeTruthy();
  });
});
