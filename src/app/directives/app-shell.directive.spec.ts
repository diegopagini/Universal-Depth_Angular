import { AppShellDirective } from './app-shell.directive';

describe('AppShellDirective', () => {
  it('should create an instance', () => {
    const directive = new AppShellDirective();
    expect(directive).toBeTruthy();
  });
});
